apiVersion: v1
kind: Service
metadata:
  name: {{ include "arjobruijnes-personal-site.fullname" . }}
  labels:
    {{- include "arjobruijnes-personal-site.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "arjobruijnes-personal-site.selectorLabels" . | nindent 4 }}
