apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "arjobruijnes-personal-site.fullname" . }}-test-connection"
  labels:
    {{- include "arjobruijnes-personal-site.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "arjobruijnes-personal-site.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
